import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static', // Default to static for performance, can be changed to 'server' for SSR
  site: 'https://lumiaohana.com', // Placeholder URL
});
